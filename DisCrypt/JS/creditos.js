document.addEventListener('DOMContentLoaded', () => {
    const creditsDuration = 20000; // 15s + 2s de fade-in
    setTimeout(() => {
        window.location.href = "index.html"; // Redirecione para a página inicial ou outro destino
    }, creditsDuration);
});
